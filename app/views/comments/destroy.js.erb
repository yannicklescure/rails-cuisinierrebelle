if(<%= @admin %>) {
  // console.log(`#comment-${<%= @comment.id %>}`);
  comment = document.querySelector(`#comment-${<%= @comment.id %>}`);
  comment.remove();
  <% if @replies_spam %>
    <% @replies_spam.each do |reply| %>
    console.log(`#reply-${<%= reply.id %>}`);
      reply = document.querySelector(`#reply-${<%= reply.id %>}`);
      reply.remove();
    <% end %>
  <% end %>
  <% if @spams.count == 1 %>
  navbarBtn = document.querySelector('#spam');
  navbarBtn.classList.remove('text-danger');
  <% end %>
} else {
  comment = document.querySelector(`#comment-${<%= @comment.id %>}`);
  comment.classList.add('d-none');
  commentsCount = document.querySelector('#comments-count');
  <% count = 0 %>
  <% count = @recipe.comments.map { |comment| comment.replies.count }.sum %>
  <% count += @recipe.comments.count %>
  commentsCount.innerHTML = `<%= t('.comments', count: count) %>`;
}
