<%= simple_form_for([recipe, comment], remote: true) do |f| %>
  <%= f.input :content, label:false, placeholder: t('.add_a_public_comment') %>
  <%= f.button :submit, t('.comment'), class: 'btn btn-secondary' %>
<% end %>
