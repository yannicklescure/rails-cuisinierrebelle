<% @recipes.each do |r| %>
  <h2><%= r.title %></h2>
  <p>
    <% r.tag_list.each do |tag| %>
      <%= link_to tag, tagged_path(tag: tag) %>
    <% end %>
  </p>
<% end %>
<%= ActsAsTaggableOn::Tag.most_used(10) %>
