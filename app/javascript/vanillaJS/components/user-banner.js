export const userBanner = () => {
  const userName = document.querySelector('#user-name');
  const userStats = document.querySelector('#user-stats');
  // if(userName && userStats) {
  //   if(userStats.offsetWidth > userName.offsetWidth) userName.style.width = `${userStats.offsetWidth}px`;
  //   else userStats.style.width = `${userName.offsetWidth}px`;
  // }
}
